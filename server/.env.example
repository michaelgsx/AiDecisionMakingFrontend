# DynamoDB 表名（需已存在，主键为 pk + sk，均为 String）
DYNAMODB_TABLE_NAME=RiskIngest

# 区域
AWS_REGION=us-east-1

# 与前端 VITE_OPS_TOKEN 一致；不设则不做鉴权（仅本地调试用）
OPS_TOKEN=

# 可选：允许的浏览器来源，逗号分隔；不设则允许任意来源
# CORS_ORIGIN=http://localhost:5173

# 本地 API 端口
PORT=8787
